sp {propose*initialize-substate-operator-agent
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-substate-operator-agent)
}

sp {apply*initialize-substate-operator-agent
   (state <s> ^operator <op>)
   (<op> ^name initialize-substate-operator-agent)
-->
   (<s> ^name substate-operator-agent)
}
