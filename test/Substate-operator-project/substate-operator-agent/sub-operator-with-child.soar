
sp {substate-operator-agent*propose*sub-operator-with-child
   (state <s> ^name substate-operator-agent)
-->
   (<s> ^operator <op> + =)
   (<op> ^name sub-operator-with-child)
}

sp {apply*sub-operator-with-child
   (state <s> ^operator <op>)
   (<op> ^name sub-operator-with-child)
-->
   (<s>)
}
