
sp {sub-operator-with-child*propose*child-operator
   (state <s> ^name sub-operator-with-child)
-->
   (<s> ^operator <op> + =)
   (<op> ^name child-operator)
}

sp {apply*child-operator
   (state <s> ^operator <op>)
   (<op> ^name child-operator)
-->
   (<s>)
}
