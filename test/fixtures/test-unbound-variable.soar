# Test file for unbound variable validation

sp {apply*initialize-Invalid-attributes-separated-by-identifier
   (state <s> ^operator <op>)
   (<il> ^hello.world <hw>)
   (<op> ^name initialize-Invalid-attributes-separated-by-identifier)
-->
   (<s> ^name Invalid-attributes-separated-by-identifier)
}

sp {valid*production*with*proper*bindings
   (state <s> ^io <io>)
   (<io> ^input-link <il>)
   (<il> ^hello.world <hw>)
-->
   (<s> ^name valid)
}
