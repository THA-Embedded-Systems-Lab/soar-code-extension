sp {propose*initialize-vars
   (state <s> ^superstate nil
             -^name)
-->
   (<s> ^operator <o> +)
   (<o> ^name initialize-vars)
}

sp {apply*initialize-vars
   (state <s> ^operator <op>)
   (<op> ^name initialize-vars)
-->
   (<s> ^name vars)
}
