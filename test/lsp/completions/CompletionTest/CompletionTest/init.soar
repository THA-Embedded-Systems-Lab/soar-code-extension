sp {CompletionTest*propose*init
   (state <s> ^name CompletionTest)
-->
   (<s> ^operator <op> + =)
   (<op> ^name init)
}

sp {apply*init
   (state <s> ^operator <op>)
   (<op> ^name init)
-->
   (<s>)
}
